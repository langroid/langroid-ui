# Copy this file to .env and fill in your values

# Optional: OpenAI API Key (if not provided, MockLM will be used)
OPENAI_API_KEY=

# Optional: Force use of MockLM even if API keys are present
USE_MOCK_LLM=false

# Server configuration
HOST=0.0.0.0
PORT=8000